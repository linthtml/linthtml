# tag-name-match

If set, tag names must match (including case).

The following patterns are considered violations:

```html
<body></Body>
```

The following patterns are considered violations:

```html
<body></body>
```
